#include <stdio.h>

int main()
{
	int Number;
	int Results;
	printf("Enter the number:");
	scanf("%d", &Number);
	Results = Number%2 ? 0:1
	if(Result==0)
	Printf("Even");
	else
	printf("Odd");
}
